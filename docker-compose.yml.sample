version: '3.7'

services:
  nexus:
    image: teecke/nexus:0.1.0
    ports:
      - 8081:8081
    volumes:
      - sonatype-work:/sonatype-work
      
networks:
    default:
      external:
        name: platform_services

volumes:
  sonatype-work:
